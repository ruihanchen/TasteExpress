package org.chendev.tasteexpress.common.enumeration;

/**
 * Generic on/off switch used across entities.
 *
 * This is intentionally small and focused: it only models
 * whether a record is enabled/visible/active in the system.
 */
public enum EnabledStatus {
    DISABLED,
    ENABLED
}
